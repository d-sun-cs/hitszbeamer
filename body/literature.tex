% !TEX root = ../main.tex

\section{后期主要工作}

\begin{frame}[t]{后期主要工作}{把 FastTT 讲扎实，再用它恢复 KV Cache}

    \vspace{-0.4em}
    \begin{columns}[T,onlytextwidth]
        \column{0.48\textwidth}
        \begin{block}{1. 给优化方法补直接证据}
            \footnotesize
            \begin{itemize}
                \setlength{\itemsep}{4pt}
                \item 现在已有端到端吞吐和消融结果
                \item 还要直接测：缓存命中率、实际访存次数
                \item 目标：直观说明省掉的内存开销
            \end{itemize}

            \vspace{0.25em}
            \centering
            \scriptsize 产出：补充实验图表，支撑论文修改和毕业论文。
        \end{block}

        \column{0.48\textwidth}
        \begin{block}{2. 内存出问题时恢复 KV Cache}
            \footnotesize
            \begin{itemize}
                \setlength{\itemsep}{4pt}
                \item 大模型推理会把中间结果存在键值缓存（KV Cache）里
                \item 节点内存故障后，重新预填充会让 GPU 再算一遍 KV，代价高
                \item 尝试用 FastTT 解码恢复丢失的 KV，减少重新计算
            \end{itemize}

            \vspace{0.25em}
            \centering
            \scriptsize 先调研 Mooncake：以 KV Cache 为中心的管理方案，再做恢复延迟原型。
        \end{block}
    \end{columns}

\end{frame}
