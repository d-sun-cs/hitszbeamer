# 中期答辩幻灯片书写指导原则

## 总体定位

这份幻灯片面向中期答辩老师，而不是面向已经熟悉 FastTT 或 Two-tone 码的同行。默认听众知道分布式系统和纠删码的大概概念，但不一定了解 Shift-XOR、Two-tone、Ceph 纠删码插件或本文的优化细节。

讲述顺序应当是：

1. 先讲具体需求：存储系统需要可靠性和冗余，多副本可靠但占空间，纠删码用更低冗余节省空间。
2. 再讲为什么还需要新实现：传统 Reed-Solomon 码要做有限域运算，Two-tone 只做移位和异或，但缺少工程级高性能实现。
3. 再讲具体工作：实现 FastTT，解决内存访问瓶颈，集成 Ceph，给出真实吞吐结果。
4. 最后讲后续工作：补充机理实验、方法论总结、探索 KV Cache 场景和风险应对。

## 三部分结构

整份答辩稿只围绕三部分展开：

1. 研究内容与进度
   - 先用具体场景解释课题为什么有意义。
   - 用一页说明 Two-tone 的核心直觉：移位加异或生成校验块。
   - 用一页列清楚中期已经做出了什么：代码、Ceph 插件、实验、论文。

2. 已完成工作与结果
   - 先讲直观实现为什么慢：不是异或慢，而是反复搬数据慢。
   - 每个优化都用“原问题 -> 具体做法 -> 直接收益”的口径讲。
   - 实验页先给结论数字，再给表格；表格只服务于支撑结论。

3. 后期工作、困难与可行性
   - 不放时间安排。
   - 讲后续要补哪些证据、解决哪些明确瓶颈、探索哪个新增创新点。
   - 困难要配对应解决动作，最后给出能按期完成的依据。

## 表达原则

- 每页先落到具体事实，再给判断：先说“多副本占空间”“纠删码要编解码”，再说“优化目标是让编解码更快”。
- 不把报告段落搬进幻灯片。正文只保留听众需要立刻看的词，解释留给口头讲。
- 不用“先看一个真实问题”“研究路线”“关注点”这类空泛引导语，直接说对象、代价和结果。
- 除非必须讨论特定存储策略，不强调“三副本”；一般说“多副本”即可。
- 一页只保留一条事实链：对象是什么、问题是什么、FastTT 做了什么、结果是什么。
- 纯文字页最多 2--3 个 block，每个 block 最多 2--3 条短句；能用数字或名词短语表达，就不要写完整长句。
- 每页标题尽量写成听众能直接理解的问题或结论，例如“慢在反复搬数据”，不要写成“研究目标与关注点”。
- 少用抽象词：目标、路线、关注点、方法论、框架。必须使用时，要紧跟具体对象或结果。
- 少用缩写。必须出现的缩写需要在首次出现时补全含义，例如 KV Cache 写成“键值缓存（KV Cache）”。
- 先给结果，再解释原因。例如实验页先写“编码平均快 59.1%”，再列测试环境和表格。
- 技术页保持左文右图。左侧只放三到五条关键句，右侧尽量放大图。
- 图片如果承担解释任务，就必须足够大；小图只作为辅助，不作为主要证据。
- 不在一页同时讲两个互不相关的技术点。若必须对比，使用“合并前/合并后”“优化前/优化后”这种明确结构。
- 每页只回答一个问题，避免把报告段落压缩成幻灯片。

## 排版原则

- 以 16:9 Beamer 版心为准，正文优先使用 `footnotesize` 或 `scriptsize`，不要在一页塞满报告式长段落。
- 表格页不再套过多 block，避免表格太小；重要数字可以单独放成结论块。
- 三个以上数字比较时使用表格；一两个关键提升使用加粗文本。
- 图片页优先给 55%--65% 的横向空间。
- 对比图应使用裁边后的 PDF，减少白边浪费。
- 编译必须使用 `latexmk`，不能单次 `xelatex`，否则目录和侧边栏可能为空。

## 当前版本应优先修正的问题

1. 开场页要从具体存储问题切入，删除“研究路线”“本课题目标”“中期阶段关注点”等抽象表达。
2. 中期进度页要讲具体产物与结果，不讲流程箭头。
3. 技术页要强调“内存访问瓶颈”这个主线，让四项优化自然接上。
4. 实验页要更像结果展示：先列平均提升和峰值提升，再用表格支撑。
5. 后期计划页要讲具体补充实验和新增创新点，不写时间表。
